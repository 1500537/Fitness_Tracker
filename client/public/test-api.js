// Simple test to check if server is running and database is connected\nconst testAPI = async () => {\n  try {\n    console.log('Testing API connection...');\n    \n    const response = await fetch('http://localhost:3000/api/revenue/test');\n    const data = await response.json();\n    \n    console.log('✅ API Response:', data);\n    \n    if (data.success) {\n      console.log(`📊 Database has ${data.data.subscriptions} subscriptions`);\n      console.log(`👥 Database has ${data.data.users} users`);\n      console.log(`💰 Database has ${data.data.revenueRecords} revenue records`);\n    }\n    \n  } catch (error) {\n    console.log('❌ Connection failed:', error.message);\n    console.log('Make sure server is running: npm run server');\n  }\n};\n\n// Run in browser console\nif (typeof window !== 'undefined') {\n  window.testAPI = testAPI;\n  console.log('Run testAPI() in console to test connection');\n} else {\n  testAPI();\n}